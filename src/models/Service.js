export default class Service {
  constructor (id, name, description, categoryId, unitPrice) {
    this.id = id
    this.name = name
    this.description = description
    this.categoryId = categoryId
    this.unitPrice = unitPrice
  }
}
